# Copy this to .env and fill in your values

# Port to listen on
PORT=3000

# Admin API key — change this to something long and random
ADMIN_KEY=changeme-use-a-long-random-string

# Optional: enforce HTTPS-only (set to true in production behind nginx/caddy)
HTTPS_ONLY=false
